# TODO

## Функциональность

- [ ] **Пагинация** — сейчас парсится только первая страница (~25 отелей на Booking, ~20 на Trip). Добавить скролл/клик «Показать ещё» или перебор страниц.
- [ ] **Автоматическое определение координат города** — сейчас lat/lng вбиваются вручную в конфиг. Добавить geocoding через Nominatim (бесплатно) при старте.
- [ ] **Дополнительные источники** — Ostrovok.ru (русскоязычный, цены в рублях, хорошо индексирует СНГ), Hotels.com.
- [ ] **Фильтр по звёздам** — параметр `min_stars` в конфиге (сейчас парсится всё подряд).
- [ ] **Дедупликация** — один отель может появляться и с Booking, и с Trip. Нужен fuzzy-match по названию для склейки дубликатов.
- [ ] **Telegram-уведомление** по окончании парсинга — отправить сводку (кол-во отелей, диапазон цен) в личный чат.

## UX / запуск

- [ ] **Настраиваемое время ожидания капчи** — сейчас 60 секунд захардкожено. Вынести в конфиг (`captcha_wait_sec`).
- [ ] **CLI-аргументы** — принимать даты и город из командной строки (`--city`, `--checkin`, `--checkout`) без правки конфига.
- [ ] **Интерактивный режим** — задавать параметры через prompts в терминале, если конфиг не менялся.

## Качество данных

- [ ] **Парсинг цены Trip.com** — сейчас Trip часто отдаёт 0 отелей из-за изменений DOM. Нужен более устойчивый селектор или fallback на API.
- [ ] **Нормализация адресов** — строки вида «1.2 км от центра» vs «1200 м» приводить к единому числовому полю `distance_km`.
- [ ] **Количество отзывов на Booking** — парсинг `review_count` нестабилен, иногда возвращает null.

## Отчёт

- [ ] **График цен** — добавить в Excel лист с диаграммой распределения цен (ExcelJS поддерживает Charts).
- [ ] **Сравнительная таблица** — для отелей, присутствующих на обоих сайтах, показать разницу цен рядом.
- [ ] **Экспорт в Google Sheets** — через googleapis, чтобы шарить результаты без xlsx.

## Инфраструктура

- [ ] **Playwright в headless-режиме** — Booking.com почти всегда показывает капчу, но можно пробовать: добавить флаг `--headless` и автоматически откатываться в видимый режим при детекции капчи.
- [ ] **Docker-образ** — чтобы запускать на сервере без GUI (с Xvfb).
- [ ] **Кэш сессии браузера** — сохранять `storageState` между запусками, чтобы не логиниться каждый раз заново.
